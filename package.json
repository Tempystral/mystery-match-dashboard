{
  "name": "mystery-match-bot",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": {
    "packages": [
      "./packages/*"
    ]
  },
  "scripts": {
    "client": "npm --workspace=@mmd/client run",
    "server": "npm --workspace=@mmd/server run",
    "common": "npm --workspace=@mmd/common run",
    "clean": "npm run client clean && npm run server clean && npm run common clean"
  }
}